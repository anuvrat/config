[user]
	name = Anuvrat Singh
	email = singh.anuvrat@gmail.com
	signingkey = ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIDFm1sz34iMJyOq92YAueuuX3KSpqd2dK/3qbJFPkMCT

[pull]
	rebase = true

[push]
	autoSetupRemote = true

[gpg]
	format = ssh

[gpg "ssh"]
	program = /Applications/1Password.app/Contents/MacOS/op-ssh-sign

[commit]
	gpgsign = true

[core]
	pager = delta

[interactive]
	diffFilter = delta --color-only

[delta]
	navigate = true
	side-by-side = false
	line-numbers = true
	syntax-theme = Nord

[merge]
	conflictstyle = diff3

[diff]
	colorMoved = default

[alias]
	lg = log --oneline --graph --decorate --all
	lga = log --graph --abbrev-commit --decorate --format=format:'%C(blue)%h%C(reset) - %C(cyan)%aD%C(reset) %C(green)(%ar)%C(reset)%C(magenta)%d%C(reset)%n''          %C(white)%s%C(reset) %C(dim)- %an%C(reset)'
